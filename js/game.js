var lastTick = new Date().getTime();
const gameLoop = function (diff) {
	diff /= 1000;
	lastTick = new Date().getTime();
	player.points;
}
var loop = setInterval(_=>{gameLoop(new Date().getTime()-lastTick)}, 50)